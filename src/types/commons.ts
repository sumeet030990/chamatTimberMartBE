export type fetchQueryParamsType = {
  pageNumber: number;
  pageSize: number;
  search: string | object;
  sort_field: string;
  sort_order: string;
  selectedCompany: string;
  sortCondition?: any;
};
